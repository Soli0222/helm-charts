apiVersion: v1
kind: Service
metadata:
  name: {{ include "mermaid-live-editor.fullname" . }}
  labels:
    {{- include "mermaid-live-editor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mermaid-live-editor.selectorLabels" . | nindent 4 }}
