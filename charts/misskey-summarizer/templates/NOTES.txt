Misskey Summary CronJob has been deployed!

Schedule: {{ .Values.cronjob.schedule }} ({{ .Values.cronjob.timeZone }})

The CronJob will run daily at the specified schedule and:
1. Fetch notes from Misskey instance: {{ .Values.env.misskeyInstanceUrl }}
2. Summarize them using OpenAI API
3. Post the summary to Discord

{{- if .Values.secrets.existingSecret }}
Using existing secret: {{ .Values.secrets.existingSecret }}
{{- else }}
A new secret has been created: {{ include "misskey-summarizer.secretName" . }}
{{- end }}

To check the CronJob status:
  kubectl get cronjob {{ include "misskey-summarizer.fullname" . }} -n {{ .Release.Namespace }}

To view recent jobs:
  kubectl get jobs -l app.kubernetes.io/instance={{ .Release.Name }} -n {{ .Release.Namespace }}

To view logs from the last job:
  kubectl logs -l app.kubernetes.io/instance={{ .Release.Name }} -n {{ .Release.Namespace }} --tail=100
